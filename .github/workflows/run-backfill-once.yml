# Одноразовая диагностика токена и запуск восстановления через специальный PR.
name: Одноразовый запуск восстановления релизов

on:
  pull_request:
    types:
      - opened

permissions:
  actions: write
  contents: read
  pull-requests: write

jobs:
  dispatch:
    if: >-
      github.event.pull_request.head.repo.full_name == github.repository &&
      github.event.pull_request.title == 'RUN: восстановить релизы'
    name: Проверить токен и запустить восстановление
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      GH_TOKEN: ${{ github.token }}
      GH_REPO: ${{ github.repository }}
      PR_NUMBER: ${{ github.event.pull_request.number }}
    steps:
      - name: Проверить RELEASE_TOKEN
        shell: bash
        env:
          RELEASE_TOKEN: ${{ secrets.RELEASE_TOKEN }}
        run: |
          set -euo pipefail
          if [[ -z "$RELEASE_TOKEN" ]]; then
            gh pr comment "$PR_NUMBER" --body "Одноразовый запуск остановлен: Actions secret \`RELEASE_TOKEN\` отсутствует. Значение секрета не раскрывалось."
            exit 1
          fi
          gh pr comment "$PR_NUMBER" --body "Actions secret \`RELEASE_TOKEN\` найден. Запускаю workflow восстановления релизов."

      - name: Запустить основной workflow
        shell: bash
        run: |
          set -euo pipefail
          gh workflow run backfill-releases.yml --ref main
          gh pr comment "$PR_NUMBER" --body "Workflow восстановления релизов успешно поставлен в очередь."
